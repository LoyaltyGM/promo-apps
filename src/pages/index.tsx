import * as React from "react";
import type { NextPage } from "next";
import Lootbox from "./lootbox";

const Home: NextPage = () => {
    return (
        <div>
            <Lootbox />
        </div>
    );
};

export default Home;
